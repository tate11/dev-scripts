#!/bin/bash
###############################################
#Include file lib in same dir as this file
. $(dirname $0)"/lib"

msgrun "Status of this repo"
git status

msgrun "Pulling scripts odoo v8 adhoc"
git pull

msgrun "Installing scripts odoo v8 adhoc"
cp sd /usr/bin
cp lib /usr/bin

msgdone "Scripts installed"
